[project]
name = "mcp-autonomous-trading-agents"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openai-agents",
    "openai",
    "mcp",
    "python-dotenv",
    "pydantic",
    "polygon-api-client",
    "requests",
    "gradio",
    "pandas",
    "plotly",
]
